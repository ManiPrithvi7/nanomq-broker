# NanoMQ 0.24.x — Plain MQTT on 1883 (staging / fallback only)
# No TLS; no certs required.

listeners.tcp {
  bind = "0.0.0.0:1883"
}

listeners.ssl {
  enable = false
}

listeners.ws {
  enable = false
}

http_server {
  enable = false
}

log {
  level = warn
  to    = [console]
}
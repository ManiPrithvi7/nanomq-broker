# =============================================================================
# Railway — Caddy L4 TCP proxy (caddy-proxy service)
# Public edge for broker.withproof.io:8883 → private NanoMQ on port 8883
# See caddy-proxy/README.md for full deploy steps.
# =============================================================================

PORT=8883
BROKER_INTERNAL_HOST=${{adequate-appreciation.RAILWAY_PRIVATE_DOMAIN}}
BROKER_INTERNAL_PORT=8883

# Caddy L4 TCP proxy — deploy as its own Railway service.
# Railway → New Service → same GitHub repo → Settings → Root Directory = caddy-proxy

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
